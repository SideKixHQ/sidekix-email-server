{
  "name": "sidekix-email-server",
  "version": "1.0.0",
  "description": "SideKix email sending server — connects the portal and Make to SendGrid",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
